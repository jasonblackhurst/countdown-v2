name: countdown_console
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  countdown_core:
    path: ../countdown_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
