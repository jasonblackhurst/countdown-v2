name: countdown_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.8

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  shelf_web_socket: ^2.0.0
  uuid: ^4.5.1
  web_socket_channel: ^3.0.2
  countdown_core:
    path: ../../packages/countdown_core

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
